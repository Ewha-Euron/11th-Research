# 2주차 예습과제 — 순환 신경망과 트랜스포머

> **작성자** 유다연
> **교재** 『(파이토치 트랜스포머를 활용한) 자연어 처리와 컴퓨터 비전 심층학습』
> **범위** 6장 순환 신경망 (pp. 304~335) / 7장 트랜스포머 (pp. 357~387)
> **논문** Vaswani et al., *Attention Is All You Need*, NeurIPS 2017
>
> 교재 범위 중간의 실습 코드는 복습과제 범위이므로 본 문서에서는 개념과 수식, 차원 규칙 중심으로 정리함.

---

## 목차

- [Part 1. 개념정리](#part-1-개념정리)
  - [1. 순환 신경망 (6장)](#1-순환-신경망-6장)
  - [2. 트랜스포머 (7장)](#2-트랜스포머-7장)
  - [3. RNN 계열과 트랜스포머 비교](#3-rnn-계열과-트랜스포머-비교)
- [Part 2. 논문 리뷰 — Attention Is All You Need](#part-2-논문-리뷰--attention-is-all-you-need)

---

# Part 1. 개념정리

## 1. 순환 신경망 (6장)

### 1.1 연속형 데이터와 자연어

- **연속형 데이터(sequential data)**: 특정 시점 $t$의 데이터가 이전 시점 $t-1, t-2, \dots$의 데이터에 영향을 받는 데이터. 일일 주가처럼 시점 간 독립성이 성립하지 않는 데이터가 여기에 해당함.
- **자연어의 연속성**: 문장 안에서 단어가 순서대로 등장하고, 각 단어가 앞선 단어의 영향을 받아 의미를 형성하므로 자연어도 연속형 데이터의 일종임.
  - 예) "금요일이 지나면 \_\_\_" → 앞선 문맥만으로 '토요일', '주말' 등을 유추 가능.
  - 예) "월요일이 지나면 화요일이고, … 금요일이 지나면 \_\_\_" → 앞선 패턴('요일 나열')까지 고려하면 '주말'보다 '토요일'이 더 자연스러운 예측.
- **핵심 시사점**: 긴 문장일수록 앞뒤 단어 사이의 **상관관계(correlation)** 가 강해지므로, 자연어 처리에서는 문맥과 상호작용 자체를 모델링해야 정확한 의미 파악이 가능함.

### 1.2 순환 신경망의 기본 구조

- **정의**: 순환 신경망(Recurrent Neural Network, RNN)은 연속형 데이터를 처리하기 위해 고안된 신경망으로, 이전에 처리한 정보를 다음 단계로 전달해 **과거 상태를 기억한 채 현재 상태를 예측**하는 구조임.
- **셀(cell)**: 각 시점의 입력을 받아 은닉 상태와 출력값을 계산하는 노드. 이전 시점의 은닉 상태 $h_{t-1}$을 입력으로 받아 현재 시점의 은닉 상태 $h_t$를 계산하는 **재귀적 구조**를 가지므로 '순환' 신경망이라 불림.
- **은닉 상태(hidden state)**: 각 시점까지 처리한 정보를 압축해 저장하는 벡터. 시점마다 갱신되며 다음 시점으로 전달됨.

**수식 6.11 — 은닉 상태**

$$h_t = \sigma_h(h_{t-1},\, x_t) = \sigma_h\!\left(W_{hh}h_{t-1} + W_{xh}x_t + b_h\right)$$

**수식 6.12 — 출력값**

$$y_t = \sigma_y(h_t) = \sigma_y\!\left(W_{hy}h_t + b_y\right)$$

| 기호 | 의미 |
|---|---|
| $\sigma_h$ | 은닉 상태 활성화 함수 (기본값 `tanh`) |
| $\sigma_y$ | 출력값 활성화 함수 |
| $W_{hh}$ | 이전 시점 은닉 상태 $h_{t-1}$에 대한 가중치 |
| $W_{xh}$ | 현재 입력 $x_t$에 대한 가중치 |
| $W_{hy}$ | 현재 은닉 상태 $h_t$에 대한 출력 가중치 |
| $b_h, b_y$ | 은닉 상태 / 출력값의 편향 |

- **동작 요약**: 시점 $t$마다 $(x_t, h_{t-1}) \rightarrow h_t \rightarrow y_t$ 순으로 계산되며, 이전 시점 정보가 $h_{t-1}$을 통해 계속 누적됨. 이 때문에 입력 시퀀스의 패턴을 반영한 예측이 가능함.
- **가중치 공유**: $W_{hh}, W_{xh}, W_{hy}$는 모든 시점에서 동일하게 재사용됨. 따라서 시퀀스 길이가 달라져도 동일한 파라미터로 처리 가능함.

### 1.3 순환 신경망의 구조 유형

입출력 시퀀스 개수 조합에 따라 네 가지 구조로 나뉨.

| 구조 | 입력 → 출력 | 대표 작업 |
|---|---|---|
| 일대일 | 1 → 1 | 가장 단순한 기본 구조 |
| **일대다(One-to-Many)** | 1 → N | 이미지 캡셔닝(이미지 → 설명 문장), 품사 예측 |
| **다대일(Many-to-One)** | N → 1 | 감성 분류(긍정/부정), 문장 분류, 자연어 추론 |
| **다대다(Many-to-Many)** | N → M | 기계 번역, 음성 인식 |

- **일대다 구조 유의점**: 출력 시퀀스의 길이를 미리 알아야 하므로, 입력 시퀀스를 처리하면서 출력 길이를 예측하는 모듈을 함께 구현해야 함.
- **다대다 구조의 실체**: 입력과 출력의 길이가 다를 수 있으며, 길이를 맞추기 위한 패딩·절단 전처리가 필요함. 일반적으로 **시퀀스-시퀀스(Seq2Seq)** 구조로 구현됨.
  - **인코더(encoder)**: 입력 시퀀스를 처리해 고정 크기 벡터(문맥 벡터)로 압축.
  - **디코더(decoder)**: 그 벡터를 입력받아 출력 시퀀스를 생성.
  - → 이 "고정 크기 벡터에 문장 전체를 욱여넣는" 구조가 이후 어텐션 메커니즘과 트랜스포머가 해결하려 한 병목임.

### 1.4 양방향 순환 신경망 (BiRNN)

- **정의**: 시간 방향을 양방향으로 처리하도록 고안된 구조. 이전 시점 $t-1$뿐 아니라 **이후 시점 $t+1$의 은닉 상태도 함께 사용**함.
- **필요성**: 대부분의 연속형 데이터는 이후 시점 데이터와도 큰 상관관계를 가짐.
  - 예) "인생은 B와 \_\_\_ 사이의 C다." → 앞부분('인생은 B와')만으로는 예측이 어렵지만, 뒷부분('사이의 C다')까지 보면 'D'라는 답이 자연스럽게 도출됨.
- **구현 방식**: 입력 데이터를 순방향으로 한 번, 역방향으로 한 번 읽어 두 은닉 상태를 함께 활용함. 따라서 출력 차원이 단방향 대비 2배가 됨.

### 1.5 다중 순환 신경망 (Stacked RNN)

- **정의**: 여러 개의 순환 신경망 층을 쌓아 구성한 모델. 다층 퍼셉트론이 단층 퍼셉트론을 쌓아 복잡한 문제를 해결한 것과 동일한 발상임.
- **동작**: 각 층의 출력값이 다음 층의 입력으로 전달됨.
- **장점**: 데이터의 다양한 특징을 추출할 수 있고, 층이 깊어질수록 더 복잡한 패턴 학습이 가능함.
- **단점**: 층이 많아질수록 학습 시간이 길어지고 **기울기 소실 문제**가 발생할 가능성이 높아짐.

### 1.6 순환 신경망 클래스 (`torch.nn.RNN`) 주요 매개변수

| 매개변수 | 의미 |
|---|---|
| `input_size` | 입력 특성 크기 ($x_t$의 차원) |
| `hidden_size` | 은닉 상태 크기 ($h_t$의 차원) |
| `num_layers` | 층 수. 2 이상이면 다중 순환 신경망 구성 |
| `nonlinearity` | 활성화 함수 $\sigma_h$ 종류 (`tanh` / `relu`) |
| `bias` | 편향 사용 여부 |
| `batch_first` | 참이면 `[배치 크기, 시퀀스 길이, 입력 특성 크기]`, 거짓이면 `[시퀀스 길이, 배치 크기, 입력 특성 크기]` |
| `dropout` | 과대적합 방지용 드롭아웃 확률 |
| `bidirectional` | 양방향 처리 여부 |

**입출력 차원 규칙** (`batch_first=True` 기준)

| 텐서 | 형태 |
|---|---|
| 입력값 | `[배치 크기, 시퀀스 길이, 입력 특성 크기]` |
| 초기 은닉 상태 $h_0$ | `[계층 수 × (양방향 여부 + 1), 배치 크기, 은닉 상태 크기]` |
| 출력값 | `[배치 크기, 시퀀스 길이, (양방향 여부 + 1) × 은닉 상태 크기]` |
| 최종 은닉 상태 | $h_0$과 동일 |

- 예) `input_size=128, hidden_size=256, num_layers=3, bidirectional=True`, 배치 4, 시퀀스 길이 6
  → 출력값 `[4, 6, 512]`, 최종 은닉 상태 `[6, 4, 256]`.
- **주의**: 배치 우선(`batch_first`) 설정에 따라 차원 순서가 바뀌므로 매개변수 설정에 주의가 필요함.

### 1.7 장단기 메모리 (LSTM)

#### (1) 등장 배경

- 1997년 셉 호흐라이터(Sepp Hochreiter)와 유르겐 슈미트후버(Jürgen Schmidhuber)가 제안함.
- **기존 RNN의 한계**
  - **장기 의존성 문제(long-term dependencies)**: 앞선 시점의 정보를 매 시점 반복 반영하는 구조라, 시퀀스가 길어지면 먼 과거의 정보가 충분히 전달되지 않음.
  - **기울기 소실/폭주**: `tanh`, ReLU 등 활성화 함수의 특성으로 인해 역전파 과정에서 기울기가 소실되거나 폭주할 가능성이 있음.
- **해결책**: **메모리 셀(memory cell)** 과 **게이트(gate)** 구조를 도입해 정보의 흐름을 명시적으로 제어함.

#### (2) 셀 상태와 세 개의 게이트

- **셀 상태(cell state) $c_t$**: 정보를 저장·유지하는 메모리 역할. 은닉 상태와 유사하게 계산되지만, **출력값 계산에 직접 사용되지 않는다**는 점이 RNN의 은닉 상태와 결정적으로 다름.
- **게이트의 공통 성질**: 세 게이트 모두 활성화 함수로 **시그모이드**를 사용함. 출력이 $[0, 1]$ 범위이므로 "정보를 얼마나 통과시킬지"의 비율로 해석됨. 1이면 완전히 통과, 0이면 완전히 차단.

| 게이트 | 역할 | 입력 |
|---|---|---|
| 망각 게이트(forget gate) | 이전 셀 상태에서 **어떤 정보를 삭제**할지 결정 | 현재 입력 + 이전 은닉 상태 |
| 기억(입력) 게이트(input gate) | **어떤 새로운 정보를 얼마나 추가**할지 결정 | 현재 입력 + 이전 은닉 상태 |
| 출력 게이트(output gate) | 셀 상태 중 **어떤 부분을 출력**할지 결정 | 현재 입력 + 이전 은닉 상태 + 갱신된 셀 |

#### (3) 수식 정리

**수식 6.13 — 망각 게이트**

$$f_t = \sigma\!\left(W_x^{f}x_t + W_h^{f}h_{t-1} + b^{f}\right)$$

- 출력값이 1에 가까울수록 이전 정보를 많이 유지하고, 0에 가까울수록 많이 삭제함.

**수식 6.14 — 기억 게이트**

$$g_t = \tanh\!\left(W_x^{g}x_t + W_h^{g}h_{t-1} + b^{g}\right)$$
$$i_t = \sigma\!\left(W_x^{i}x_t + W_h^{i}h_{t-1} + b^{i}\right)$$

- $g_t$: **후보 기억값**. `tanh`를 사용하므로 $[-1, 1]$ 범위를 가짐.
- $i_t$: **기억 비율**. 시그모이드를 사용하므로 $[0, 1]$ 범위를 가지며, 후보 기억값을 얼마나 받아들일지 조절하는 가중치 역할을 함.
- $g_t$만으로는 "얼마나 기억할지"를 제어할 수 없기 때문에 $i_t$가 필요함.

**수식 6.15 — 메모리 셀 갱신**

$$c_t = f_t \odot c_{t-1} + g_t \odot i_t$$

- $\odot$는 **아다마르 곱(Hadamard product)**, 즉 원소별 곱셈임.
- 좌항: 이전 셀 상태를 **얼마나 유지**할지 (망각 게이트).
- 우항: 새로운 정보를 **얼마나 추가**할지 (기억 게이트).
- $f_t$가 0에 가까울수록 이전 셀 상태가 현재에 영향을 주지 않게 됨.
- **핵심**: 곱셈이 아니라 **덧셈**으로 정보를 누적하는 이 경로가 기울기 소실을 완화하는 핵심 장치임.

**수식 6.16 — 출력 게이트**

$$o_t = \sigma\!\left(W_x^{o}x_t + W_h^{o}h_{t-1} + b^{o}\right)$$

**수식 6.17 — 은닉 상태 갱신**

$$h_t = o_t \odot \tanh(c_t)$$

- $o_t \in [0, 1]$, $\tanh(c_t) \in [-1, 1]$이므로, 두 값의 아다마르 곱으로 "현재 은닉 상태가 이전 은닉 상태의 정보를 얼마나 반영할지"를 제어할 수 있게 됨.

#### (4) 장단기 메모리 클래스 (`torch.nn.LSTM`)

- `nn.RNN`과 거의 동일한 구조이나, **활성화 함수가 수식에 고정돼 있으므로 `nonlinearity` 매개변수가 없음**.
- 대신 **`proj_size`(투사 크기)** 매개변수를 제공함.
  - 은닉 상태를 **선형 투사(linear projection)** 로 다른 차원에 매핑해 출력 차원을 줄이는 기능.
  - 0이면 은닉 상태 크기를 그대로 유지하며, 모델 크기·복잡도 조절을 위해 보통 `hidden_size`보다 작은 값으로 설정함.

**입출력 차원 규칙**

| 텐서 | 형태 |
|---|---|
| 입력값 | `[배치 크기, 시퀀스 길이, 입력 특성 크기]` |
| 초기 은닉 상태 $h_0$ | `[계층 수 × (양방향 여부 + 1), 배치 크기, proj_size > 0 ? proj_size : 은닉 상태 크기]` |
| 초기 셀 상태 $c_0$ | `[계층 수 × (양방향 여부 + 1), 배치 크기, 은닉 상태 크기]` ← **투사 영향 없음** |
| 출력값 | `[배치 크기, 시퀀스 길이, (양방향 여부 + 1) × (proj_size 또는 은닉 상태 크기)]` |

- 예) `input_size=128, hidden_size=256, num_layers=3, bidirectional=True, proj_size=64`, 배치 4, 시퀀스 길이 6
  → 출력값 `[4, 6, 128]`, $h_n$ `[6, 4, 64]`, $c_n$ `[6, 4, 256]`.
- **주의점 2가지**: ① 은닉 상태와 셀 상태를 **튜플**로 묶어 전달해야 함. ② 투사 크기 적용 여부에 따라 $h$와 $c$의 차원이 서로 달라짐.

---

## 2. 트랜스포머 (7장)

### 2.0 트랜스포머 기반 모델 계보 (표 7.1)

| 모델 | 학습 구조 | 학습 방법 | 학습 방향성 |
|---|---|---|---|
| BERT | 인코더 | 오토 인코딩 | 양방향 |
| GPT | 디코더 | 자기 회귀 | 단방향 |
| BART | 인코더 + 디코더 | 오토 인코딩 + 자기 회귀 | 양방향 + 단방향 |
| ELECTRA | 인코더 + 판별기 | 오토 인코딩 + 대체 토큰 탐지 | 양방향 |
| T5 | 인코더 + 디코더 | 오토 인코딩 + 자기 회귀 + 다양한 NLP 작업 학습 | 양방향 |

- **읽는 법**: 인코더만 쓰면 양방향 문맥 이해에, 디코더만 쓰면 단방향 생성에 특화됨. 이후 등장하는 사전학습 모델 대부분이 트랜스포머의 인코더/디코더 중 무엇을 취했는지로 구분 가능함.

### 2.1 트랜스포머 개요

- **정의**: 시퀀스 모델 중 하나로, 순환 신경망이나 합성곱 신경망과 달리 **어텐션 메커니즘만으로** 시퀀스 임베딩을 표현하는 모델.
- **장점**
  - 순환 구조를 제거해 **병렬 처리**가 가능 → 학습 속도가 빠르고 대규모 데이터세트에서 높은 성능을 보임.
  - 임베딩 과정에서 **문장 전체 정보를 한 번에 고려**하므로 문장이 길어져도 성능이 유지됨.
- **소스/타깃 구분**: 입력 시퀀스를 소스(source)와 타깃(target) 데이터로 나눠 처리함. 영→한 번역이라면 참조 언어인 영어가 소스, 생성 언어인 한국어가 타깃임.
- **전체 흐름**
  1. 인코더가 **위치 인코딩된 소스 입력 임베딩**을 받아 트랜스포머 블록을 거쳐 출력 벡터를 생성.
  2. 디코더가 **위치 인코딩된 타깃 입력 임베딩**을 받고, 마스크 멀티 헤드 어텐션으로 타깃 시퀀스를 순차 생성하되 인코더의 출력 벡터를 참조해 표현을 고도화.
  3. 최종 디코더 출력 벡터를 선형 변환해 어휘 사전에 대한 예측 확률을 계산.
- **트랜스포머 블록의 구성**: 인코더·디코더 모두 여러 개의 트랜스포머 블록으로 이뤄지며, 각 블록은 **멀티 헤드 어텐션 + 순방향 신경망**으로 구성됨.

### 2.2 입력 임베딩과 위치 인코딩

#### (1) 입력 임베딩

- 어휘 사전 크기를 $V$, 입력 임베딩 차원을 $d$라 할 때, 한 토큰의 원-핫 벡터는 $[1, V]$ 크기를 가지며 임베딩 행렬 $[V, d]$와의 곱으로 $[1, d]$ 크기 벡터로 변환됨.
- 일반화하면, $N$개의 문장이 최대 $S$개의 토큰 길이를 가질 때 $[N, S, V]$ 원-핫 텐서가 $[N, S, d]$ 임베딩 텐서로 변환됨.
- 이 임베딩 텐서는 트랜스포머의 **모든 계층에서 공유**되는 텐서로 사용됨.

#### (2) 위치 인코딩이 필요한 이유

- 트랜스포머는 입력 시퀀스를 **병렬 구조로 한 번에** 처리하므로, RNN과 달리 단어의 **순서 정보가 구조적으로 주어지지 않음**.
- 따라서 위치 정보를 담은 벡터를 임베딩 벡터에 **더해서(sum)** 순서 정보를 명시적으로 주입해야 함.

**수식 7.1 — 위치 인코딩**

$$PE_{(pos,\,2i)} = \sin\!\left(\frac{pos}{10000^{2i/d_{model}}}\right)$$

$$PE_{(pos,\,2i+1)} = \cos\!\left(\frac{pos}{10000^{2i/d_{model}}}\right)$$

- $pos$: 입력 시퀀스에서 해당 단어의 위치, $i$: 임베딩 벡터의 차원 인덱스.
- 차원 인덱스가 **짝수면 sin**, **홀수면 cos** 함수를 적용함.
- $1/10000^{2i/d_{model}}$은 위치를 각도 정보로 변환하는 **스케일링 인자**로, 각 위치에 대한 주기적 신호를 생성함.
- **효과**: 토큰 위치마다 서로 다른 패턴의 벡터가 더해지므로, 모델이 각 토큰의 위치 정보를 학습할 수 있게 됨. 차원별로 주기가 달라 위치를 중복 없이 표현 가능함.
- 계산된 위치 인코딩은 입력 임베딩과 더해진 뒤 드롭아웃이 적용되며, 학습 대상 파라미터가 아니므로 버퍼로 등록해 갱신되지 않게 설정함.

### 2.3 특수 토큰

| 토큰 | 의미 | 역할 |
|---|---|---|
| `BOS` | Beginning of Sentence | 문장의 시작 표시 |
| `EOS` | End of Sentence | 문장의 끝 표시 |
| `UNK` | Unknown | 어휘 사전에 없는 단어 |
| `PAD` | Padding | 문장 길이를 일정하게 맞추기 위한 빈 값 |

- 특수 토큰으로 모델이 시퀀스의 시작과 끝을 인식할 수 있게 하며, 마스킹을 통해 일부 입력을 무시할 수 있음.
- 예) 번역 모델의 디코더에서는 현재 위치 **이후**의 토큰을 마스크해 이전 토큰만 참조하게 함.

### 2.4 트랜스포머 인코더

인코더 블록의 연산 순서:
**멀티 헤드 어텐션 → 덧셈 & 정규화 → 순방향 신경망 → 덧셈 & 정규화**

#### (1) 쿼리 · 키 · 값 벡터

- 입력 텐서 $[N, S, d]$를 **선형 변환**해 세 개의 임베딩 벡터를 생성함.

| 벡터 | 기호 | 의미 |
|---|---|---|
| 쿼리(Query) | $v^q$ | 현재 시점에서 참조하고자 하는 정보의 기준. "질문"이 되는 벡터 |
| 키(Key) | $v^k$ | 쿼리와 비교되는 대상. 입력 시퀀스에서 탐색되는 벡터 |
| 값(Value) | $v^v$ | 어텐션 스코어를 얼마나 반영할지 설정하는 가중치 역할의 실제 정보 |

- 세 벡터는 초기에는 비슷한 값을 갖지만, 학습을 통해 각자 의도한 의미의 값을 갖게 됨.

#### (2) 셀프 어텐션 (수식 7.2)

$$score(v^q, v^k) = \mathrm{softmax}\!\left(\frac{(v^q)^{T} \cdot v^{k}}{\sqrt{d}}\right)$$

- 쿼리와 키의 **내적**으로 $[N, S, S]$ 크기의 **어텐션 스코어 맵**을 계산함.
- $\sqrt{d}$(벡터 차원의 제곱근)로 나눠 보정하는 이유: 벡터 차원이 커질수록 내적 값이 함께 커지는 문제를 완화하기 위함.
- 보정된 스코어에 소프트맥스를 적용해 확률로 재표현하고, 이를 값 벡터와 내적해 셀프 어텐션된 벡터를 생성함.
- **의미**: 이렇게 계산된 어텐션 결과가 각 단어의 임베딩 벡터를 대체함. 즉, **입력 시퀀스 단어 사이의 상호작용을 반영해 임베딩 벡터가 갱신됨**.

#### (3) 멀티 헤드 어텐션

- 셀프 어텐션을 여러 번 수행해 **여러 개의 헤드**를 만들고, 각 헤드가 독립적으로 어텐션을 수행한 뒤 결과를 합치는 방식.
- **차원 변화**: 입력 $[N, S, d]$에서 $k$개의 헤드를 만들려면 헤드 차원 축을 생성해 $[N, k, S, d/k]$ 형태를 구성함. 이는 $k$개의 셀프 어텐션된 $[N, S, d/k]$ 텐서를 의미함.
- 생성된 $k$개의 벡터는 임베딩 차원 축으로 다시 **병합(concatenation)** 되어 $[N, S, d]$ 형태로 출력됨.
- **효과**: 헤드마다 서로 다른 관점의 관계를 포착할 수 있음. 전체 차원을 $k$로 쪼개므로 연산량은 단일 헤드와 비슷하게 유지됨.

#### (4) 덧셈 & 정규화, 순방향 신경망

- **덧셈 & 정규화(Add & Norm)**
  - 멀티 헤드 어텐션 통과 **이전** 입력 $[N, S, d]$와 통과 **이후** 출력 $[N, S, d]$를 더함 → **잔차 연결**로 학습 시 기울기 소실 완화.
  - 이후 임베딩 차원 축으로 **계층 정규화(Layer Normalization)** 를 적용.
- **순방향 신경망(Feed-Forward Network)**
  - 선형 임베딩과 활성화 함수로 구성된 인공 신경망, 또는 1차원 합성곱을 적용할 수 있음.
  - 어텐션으로 산출된 임베딩 벡터를 더욱 고도화하는 역할.
  - 이후 다시 덧셈 & 정규화 수행.

#### (5) 블록의 반복과 인코더-디코더 연결

- 인코더는 여러 개의 인코더 블록으로 구성되며, 이전 블록의 출력이 다음 블록의 입력이 되면서 입력 시퀀스의 정보가 점차 **추상화**됨.
- **마지막 인코더 블록의 출력 벡터**는 디코더의 멀티 헤드 어텐션에서 참조되는 **키·값 벡터**로 활용됨. 이 방식으로 인코더와 디코더가 정보를 공유함.

### 2.5 트랜스포머 디코더

디코더 블록의 연산 순서:
**마스크 멀티 헤드 어텐션 → 덧셈 & 정규화 → 멀티 헤드 어텐션(인코더-디코더) → 덧셈 & 정규화 → 순방향 신경망 → 덧셈 & 정규화**

#### (1) 마스크 멀티 헤드 어텐션과 인과성

- 인코더의 멀티 헤드 어텐션이 디코더에서는 **인과성(causality)** 을 반영한 **마스크 멀티 헤드 어텐션**으로 대체됨.
- **마스킹 규칙**: 첫 번째 쿼리 벡터는 첫 번째 키 벡터만, 두 번째 쿼리 벡터는 첫·두 번째 키 벡터만 볼 수 있게 마스크를 씌움. 일반화하면 $i$번째 쿼리 벡터는 $i+1$ 이상의 키 벡터에 어텐션을 수행할 수 없음.
- **구현 방식**: 마스크 영역에 수치적으로 매우 작은 값인 $-\infty$를 더함 → 소프트맥스 계산 시 해당 영역의 어텐션 가중치가 0이 됨 → 미래 시점 토큰의 정보를 참조하지 않게 됨.
- **목적**: 모델이 현재 예측하려는 위치 **이전**의 토큰만 참고하게 제한해, 미래 정보가 현재 예측에 새어 들어가지 않게 함.

#### (2) 인코더-디코더 어텐션

- 디코더의 두 번째 멀티 헤드 어텐션에서는 역할이 나뉨.

| 벡터 | 출처 |
|---|---|
| 쿼리 | **타깃 데이터** (디코더 입력 임베딩 + 위치 인코딩) |
| 키 · 값 | **소스 데이터** (마지막 인코더 블록의 출력) |

- 쿼리·키·값으로 어텐션 스코어 맵을 계산하고 소프트맥스로 어텐션 가중치를 구한 뒤, 가중치와 값 벡터를 가중합해 출력 벡터를 얻음.
- **의미**: "지금 생성하려는 타깃 토큰이 소스 문장의 어느 부분과 관련 있는가"를 계산하는 단계.

#### (3) 출력 산출과 순차 생성

- 디코더도 여러 블록으로 구성되며, 이전 블록의 산출물이 다음 블록의 입력으로 전달됨.
- 마지막 디코더 블록의 출력 텐서 $[N, S, d]$에 선형 변환과 소프트맥스를 적용해 각 타깃 시퀀스 위치마다 예측 확률을 계산함.
- **순차 생성 방식**: 추론 시에는 `BOS`로 시작해 아직 생성되지 않은 자리를 `PAD`로 채운 뒤, 한 스텝마다 토큰을 하나씩 채워 나가며 `EOS`가 나올 때까지 반복함.

| 추론 순서 | 디코더 입력 (일부) | 디코더 출력 (일부) |
|---|---|---|
| 1 | `[BOS]`, `[PAD]`, `[PAD]`, … | `[BOS]`, 'ChatGPT', `[PAD]`, … |
| 2 | `[BOS]`, 'ChatGPT', `[PAD]`, … | `[BOS]`, 'ChatGPT', '는', `[PAD]`, … |
| 3 | `[BOS]`, 'ChatGPT', '는', `[PAD]`, … | `[BOS]`, 'ChatGPT', '는', '트랜스포머', … |
| ⋮ | ⋮ | ⋮ |
| 7 | `[BOS]`, …, '있다', `[PAD]` | `[BOS]`, …, '있다', `[EOS]` |

> **정리**: 학습 시에는 정답 시퀀스를 한 번에 넣고 마스크로 인과성을 보장해 **병렬 학습**이 가능하지만, 추론 시에는 결국 한 토큰씩 순차 생성해야 함. 트랜스포머가 "빠르다"는 말은 **학습** 기준임을 유의할 것.

### 2.6 마스크 정리

트랜스포머에서 사용하는 마스크는 목적이 다른 두 종류로 나뉨.

| 종류 | 형태 | 목적 |
|---|---|---|
| 소스 마스크 `src_mask` | `[소스 시퀀스 길이, 소스 시퀀스 길이]` | 인코더 셀프 어텐션 범위 지정 (보통 전체 참조 허용) |
| 타깃 마스크 `tgt_mask` | `[타깃 시퀀스 길이, 타깃 시퀀스 길이]` | **인과성 보장** (상삼각 마스크) |
| 메모리 마스크 `memory_mask` | `[타깃 시퀀스 길이, 소스 시퀀스 길이]` | 인코더 출력에 대한 마스크 |
| 키 패딩 마스크 `*_key_padding_mask` | `[배치 크기, 시퀀스 길이]` | **패딩 토큰 무시** (실제 의미가 없는 위치의 가중치를 0으로) |

- **어텐션 마스크의 값 규약**: 참조 허용 위치는 `0.0`, 제외할 위치는 `-inf`를 채움. `-inf`가 더해지면 소프트맥스를 거친 어텐션 스코어가 0에 수렴하므로 해당 토큰이 계산에서 배제됨.
- **타깃 마스크의 생성**: 1로 채워진 행렬에 상삼각 행렬 연산을 적용한 뒤 전치하여, 0인 값은 `-inf`로, 1인 값은 `0.0`으로 치환해 만듦.
- **키 패딩 마스크의 값 규약**: 불리언 텐서로, `False`이면 실제 토큰이 존재하는 위치, `True`이면 패딩 토큰으로 채워진 위치임.
- **타깃 시프트**: 타깃 입력값은 `target[:-1]`, 타깃 출력값은 `target[1:]`로 한 칸 시프트해, "이전 토큰들이 주어졌을 때 다음 토큰을 예측"하는 형태로 학습함.

### 2.7 트랜스포머 클래스 (`torch.nn.Transformer`) 주요 매개변수

| 매개변수 | 기본값 | 의미 |
|---|---|---|
| `d_model` | 512 | 모델의 입출력 차원. 임베딩 차원 크기와 동일 |
| `nhead` | 8 | 멀티 헤드 어텐션의 헤드 개수. 많을수록 병렬 처리 능력이 커지지만 파라미터 수도 증가 |
| `num_encoder_layers` | 6 | 인코더 계층 수 |
| `num_decoder_layers` | 6 | 디코더 계층 수 |
| `dim_feedforward` | 2048 | 순방향 신경망의 은닉층 크기. 각 입력 위치에 **독립적으로** 적용됨 |
| `dropout` | 0.1 | 인코더·디코더 계층에 적용되는 드롭아웃 비율 |
| `activation` | `relu` | 순방향 신경망의 활성화 함수 |
| `layer_norm_eps` | 1e-05 | 계층 정규화 시 분모에 더해지는 입실론 |

- 계층 개수가 많을수록 복잡한 문제를 해결할 수 있으나 **과대적합** 위험이 커짐.
- 순방향 메서드의 소스·타깃은 기본적으로 `[시퀀스 길이, 배치 크기, 임베딩 차원]` 형태로 입력받으며, 반환값은 `[타깃 시퀀스 길이, 배치 크기, 임베딩 차원]` 형태임. (생성기 계층을 거치면 마지막 차원이 타깃 어휘 사전 크기로 바뀜)

### 2.8 추론: 그리디 디코딩

- **그리디 디코딩(greedy decoding)**: 디코더가 생성한 확률 분포에서 **매 시점 가장 확률이 높은 단어 하나를 선택**해 디코딩을 진행하는 방식.
- **동작 순서**
  1. `model.encode`로 소스 문장을 인코딩해 마지막 인코더 블록의 출력 벡터(memory)를 추출. 이때 소스 마스크는 모든 토큰이 어텐션될 수 있게 0으로 설정.
  2. `BOS`부터 시작해 `model.decode`로 다음 토큰 확률을 계산하고, 가장 확률이 높은 토큰 인덱스를 선택해 시퀀스에 이어 붙임.
  3. 최대 길이에 도달하거나 `EOS`를 예측할 때까지 반복.
- **최대 길이 설정**: 보통 `소스 토큰 개수 + 5`로 설정함. 생성된 문장이 소스 문장보다 약간 길어지는 경우가 많기 때문임.
- **한계**: 매 시점 국소적으로 최선인 선택이 전체적으로 최적이라는 보장이 없음. → 실제 번역 모델은 **빔 서치(beam search)** 를 사용하는 경우가 많음.

---

## 3. RNN 계열과 트랜스포머 비교

| 항목 | RNN / LSTM | Transformer |
|---|---|---|
| 순서 정보 처리 | 구조 자체(재귀)에 내재 | 위치 인코딩으로 **명시적 주입** |
| 병렬화 | 시점 순차 계산이라 불가능 | 학습 시 시퀀스 전체 병렬 처리 가능 |
| 장거리 의존성 | 거리에 비례해 정보 손실 (LSTM이 완화) | 어텐션으로 **거리와 무관하게** 직접 연결 |
| 기울기 소실 대응 | 게이트 + 셀 상태의 덧셈 경로 | 잔차 연결 + 계층 정규화 |
| 문맥 반영 범위 | 이전 시점 중심 (BiRNN은 양방향) | 문장 전체를 한 번에 |
| 주된 비용 | 시퀀스 길이에 비례한 **순차 연산** | 시퀀스 길이의 **제곱**에 비례한 어텐션 연산 |

- **연결 고리**: 6장에서 LSTM이 게이트로 "무엇을 기억할지"를 학습했다면, 7장의 어텐션은 "어디를 볼지"를 학습함. 문제의식(장기 의존성)은 같고 해법의 층위가 달라진 것으로 이해할 수 있음.

---

# Part 2. 논문 리뷰 — Attention Is All You Need

> Ashish Vaswani, Noam Shazeer, Niki Parmar, Jakob Uszkoreit, Llion Jones, Aidan N. Gomez, Łukasz Kaiser, Illia Polosukhin
> *31st Conference on Neural Information Processing Systems (NIPS 2017)*, arXiv:1706.03762

## 1. 문제의식 — 순차성이라는 병목

2017년 당시 시퀀스 변환(sequence transduction) 분야의 표준은 LSTM이나 GRU를 인코더-디코더로 쌓고, 거기에 어텐션을 덧붙이는 구조였다. 저자들이 지적하는 문제는 성능이 아니라 **구조 자체에 내재된 순차성**이다. 순환 모델은 은닉 상태 $h_t$를 $h_{t-1}$과 위치 $t$의 입력으로부터 계산하기 때문에, 위치를 계산 시간 단계에 정렬하는 방식이 강제된다. 이 순차적 성질 때문에 **하나의 학습 예제 안에서는 병렬화가 원천적으로 불가능하고**, 시퀀스가 길어지면 메모리 제약으로 예제 간 배치조차 제한된다.

이 병목을 우회하려는 시도가 없던 것은 아니다. Extended Neural GPU, ByteNet, ConvS2S는 합성곱을 기본 블록으로 써서 모든 위치의 표현을 병렬로 계산했다. 그러나 이 모델들에서 임의의 두 위치 사이의 신호를 연결하는 데 필요한 연산 수는 거리에 따라 증가한다(ConvS2S는 선형, ByteNet은 로그). 결국 먼 위치 간 의존성 학습은 여전히 어려웠다.

저자들의 답은 단순하고 과감하다. **순환과 합성곱을 전부 걷어내고 어텐션만 남기자는 것**이다. 이렇게 하면 두 위치를 연결하는 연산 수가 **상수**로 줄어든다. 제목의 "Attention Is All You Need"는 수사가 아니라 구조에 대한 정확한 서술이다.

## 2. 핵심 아이디어

### 2.1 Scaled Dot-Product Attention

어텐션은 쿼리와 키-값 쌍의 집합을 출력으로 매핑하는 함수로 정의된다. 출력은 값들의 가중합이며, 각 값에 부여되는 가중치는 쿼리와 해당 키의 호환성 함수(compatibility function)로 계산된다.

$$\mathrm{Attention}(Q, K, V) = \mathrm{softmax}\!\left(\frac{QK^{T}}{\sqrt{d_k}}\right)V$$

여기서 흥미로운 지점은 $\sqrt{d_k}$로 나누는 스케일링이다. 저자들은 각주에서 그 이유를 구체적으로 설명한다. $q$와 $k$의 각 성분이 평균 0, 분산 1인 독립 확률변수라고 가정하면, 내적 $q \cdot k = \sum_{i=1}^{d_k} q_i k_i$는 평균 0, **분산 $d_k$** 를 갖는다. 즉 $d_k$가 커질수록 내적 값의 크기가 커지고, 소프트맥스가 기울기가 극히 작은 영역으로 밀려나게 된다. 스케일링은 이 포화를 막기 위한 장치다.

가산 어텐션(additive attention)이 이론적 복잡도는 비슷한데도 채택되지 않은 이유도 명확히 밝힌다. 내적 어텐션은 고도로 최적화된 행렬 곱 코드로 구현할 수 있어 **실제로 훨씬 빠르고 메모리 효율적**이기 때문이다. 이론적 우아함이 아니라 하드웨어 친화성을 근거로 설계를 고른 점이 인상적이다.

### 2.2 Multi-Head Attention

$d_{model}$ 차원으로 어텐션을 한 번 수행하는 대신, 쿼리·키·값을 서로 다른 학습된 선형 변환으로 $h$번 투영해 병렬로 어텐션을 수행한 뒤 결과를 이어 붙이고 다시 투영한다.

$$\mathrm{MultiHead}(Q,K,V) = \mathrm{Concat}(\mathrm{head}_1, \dots, \mathrm{head}_h)W^{O}$$
$$\mathrm{head}_i = \mathrm{Attention}(QW_i^{Q}, KW_i^{K}, VW_i^{V})$$

논문은 $h=8$, $d_k = d_v = d_{model}/h = 64$를 사용한다. 각 헤드의 차원을 줄였기 때문에 **전체 연산 비용은 단일 헤드 풀 차원 어텐션과 비슷하다.** 저자들이 밝힌 도입 이유는 "단일 헤드에서는 평균화(averaging)가 서로 다른 표현 부분공간의 정보를 함께 보는 것을 방해한다"는 것이다. 여러 관점을 동시에 유지하기 위한 장치인 셈이다.

### 2.3 어텐션의 세 가지 용법

같은 멀티 헤드 어텐션 모듈이 위치에 따라 세 가지 역할로 쓰인다는 점이 이 논문 구조의 핵심이다.

| 위치 | Q | K, V | 기능 |
|---|---|---|---|
| 인코더 셀프 어텐션 | 이전 인코더 층 출력 | 동일 | 소스 내 모든 위치 간 관계 |
| 디코더 마스크 셀프 어텐션 | 이전 디코더 층 출력 | 동일 | 현재 위치까지만 참조 (자기회귀성 보존) |
| 인코더-디코더 어텐션 | 이전 디코더 층 출력 | **인코더 출력** | 타깃의 각 위치가 소스 전체를 참조 |

디코더의 자기회귀성은 소프트맥스 입력에서 허용되지 않는 연결을 $-\infty$로 마스킹하는 방식으로 구현된다. 별도의 모듈이 아니라 **스케일드 닷-프로덕트 어텐션 안에서** 처리한다는 점이 구현상 깔끔하다.

### 2.4 나머지 구성 요소

- **Position-wise FFN**: $\mathrm{FFN}(x) = \max(0, xW_1 + b_1)W_2 + b_2$. 각 위치에 독립적·동일하게 적용되며, $d_{model}=512$, $d_{ff}=2048$이다. 저자들은 이를 "커널 크기 1인 합성곱 두 개"로 볼 수도 있다고 설명한다.
- **Positional Encoding**: 사인·코사인 함수를 사용하며, 파장이 $2\pi$부터 $10000 \cdot 2\pi$까지 기하급수를 이룬다. 이 함수를 고른 이유는 임의의 고정 오프셋 $k$에 대해 $PE_{pos+k}$가 $PE_{pos}$의 **선형 함수로 표현될 수 있어** 상대적 위치로 어텐션하는 법을 모델이 쉽게 학습하리라 가정했기 때문이다. 학습된 위치 임베딩과 성능은 거의 같았지만(Table 3의 (E)행), 학습 때 보지 못한 더 긴 시퀀스로 **외삽**할 수 있으리라는 기대로 사인 방식을 택했다고 밝힌다.
- **Residual + LayerNorm**: 모든 서브층의 출력은 $\mathrm{LayerNorm}(x + \mathrm{Sublayer}(x))$ 형태다. 잔차 연결을 위해 임베딩 층을 포함한 모든 서브층의 출력 차원을 $d_{model}=512$로 통일했다.

## 3. Why Self-Attention — 논문에서 가장 설득력 있는 부분

4절은 세 가지 기준으로 층 유형을 비교한다. ① 층당 총 계산 복잡도, ② 병렬화 가능한 계산량(= 필요한 최소 순차 연산 수), ③ 장거리 의존성 간 **경로 길이(path length)**.

| 층 유형 | 층당 복잡도 | 순차 연산 | 최대 경로 길이 |
|---|---|---|---|
| Self-Attention | $O(n^2 \cdot d)$ | $O(1)$ | $O(1)$ |
| Recurrent | $O(n \cdot d^2)$ | $O(n)$ | $O(n)$ |
| Convolutional | $O(k \cdot n \cdot d^2)$ | $O(1)$ | $O(\log_k n)$ |
| Self-Attention (restricted) | $O(r \cdot n \cdot d)$ | $O(1)$ | $O(n/r)$ |

이 표가 논문의 주장을 가장 압축적으로 보여준다. 순환 층은 순차 연산이 $O(n)$이고 경로 길이도 $O(n)$인 반면, 셀프 어텐션은 둘 다 $O(1)$이다. 신호가 전파되는 경로가 짧을수록 장거리 의존성을 학습하기 쉽다는 것이 저자들의 논지이고, 이 관점에서 셀프 어텐션은 구조적으로 유리하다.

복잡도 항목도 흥미롭다. 셀프 어텐션이 순환 층보다 빠른 조건은 **$n < d$**, 즉 시퀀스 길이가 표현 차원보다 짧을 때다. 저자들은 word-piece나 byte-pair 표현을 쓰는 당시 기계 번역의 문장 표현에서는 대체로 이 조건이 성립한다고 덧붙인다. 조건부 우위라는 점을 숨기지 않고 명시한 서술이 정직하게 느껴졌다. 아주 긴 시퀀스에 대해서는 이웃 $r$개로 어텐션을 제한하는 방안을 제시하는데, 이는 경로 길이를 $O(n/r)$로 늘리는 대가를 치른다. 오늘날의 다양한 효율적 어텐션 연구가 사실상 이 각주에서 출발했다고 볼 수 있다.

부수적 이점으로 **해석 가능성**도 언급된다. 개별 어텐션 헤드가 서로 다른 작업을 수행하도록 학습되며, 상당수가 문장의 구문적·의미적 구조와 관련된 행동을 보인다는 관찰이다.

## 4. 실험 결과

### 4.1 기계 번역

- **데이터**: WMT 2014 영-독 약 450만 문장 쌍(BPE, 공유 어휘 약 37,000 토큰), 영-불 3,600만 문장(word-piece 32,000).
- **하드웨어**: NVIDIA P100 8장 한 대. base 모델은 스텝당 약 0.4초로 10만 스텝(**12시간**), big 모델은 스텝당 1.0초로 30만 스텝(**3.5일**).
- **최적화**: Adam($\beta_1=0.9$, $\beta_2=0.98$, $\epsilon=10^{-9}$)에 warmup 4,000 스텝의 **역제곱근 스케줄**을 적용했다. 학습률을 처음엔 선형 증가시키고 이후 스텝 수의 역제곱근에 비례해 감소시키는 방식이다.
- **정규화**: Residual dropout($P_{drop}=0.1$), 레이블 스무딩($\epsilon_{ls}=0.1$). 레이블 스무딩은 모델을 더 불확실하게 만들어 perplexity는 악화시키지만 정확도와 BLEU는 개선한다고 명시한다.

| 모델 | EN-DE BLEU | EN-FR BLEU | 학습 비용 (FLOPs, EN-DE) |
|---|---|---|---|
| GNMT + RL Ensemble | 26.30 | 41.16 | $1.8 \times 10^{20}$ |
| ConvS2S Ensemble | 26.36 | 41.29 | $7.7 \times 10^{19}$ |
| **Transformer (base)** | **27.3** | 38.1 | $3.3 \times 10^{18}$ |
| **Transformer (big)** | **28.4** | **41.8** | $2.3 \times 10^{19}$ |

주목할 점은 **base 모델조차 이전의 모든 단일 모델과 앙상블을 능가**했다는 것이다. 그것도 경쟁 모델들의 학습 비용에 비하면 한 자릿수 이상 적은 비용으로 달성했다. big 모델은 영-독에서 기존 최고 성능(앙상블 포함)을 2.0 BLEU 이상 앞섰다.

### 4.2 구성 요소 분석 (Table 3)

- **(A) 헤드 수**: 연산량을 고정한 채 헤드 수를 바꿨을 때, 단일 헤드는 최적 설정보다 0.9 BLEU 낮았고, 헤드가 **너무 많아도** 성능이 떨어졌다(32헤드에서 25.4). 멀티 헤드가 무조건 많을수록 좋은 것은 아니라는 점이 실험으로 확인된다.
- **(B) 키 차원 $d_k$**: $d_k$를 줄이면 성능이 나빠졌다. 저자들은 이로부터 "호환성을 판단하는 일이 쉽지 않으며, 내적보다 더 정교한 호환성 함수가 유용할 수 있다"는 해석을 내놓는다. **자기 방법의 한계를 스스로 지적한 대목**이라 인상 깊었다.
- **(C)(D)** 모델이 클수록 좋고, 드롭아웃이 과적합 방지에 매우 효과적이다.
- **(E)** 사인 위치 인코딩을 학습된 위치 임베딩으로 교체해도 결과가 거의 동일했다(4.92 → 4.92 PPL, 25.8 → 25.7 BLEU).

### 4.3 영어 구문 분석으로의 일반화

번역 전용 구조가 아님을 보이기 위해 영어 구성 구문 분석(constituency parsing)에도 적용했다. 출력이 강한 구조적 제약을 받고 입력보다 훨씬 길어지는, 성격이 꽤 다른 과제다. WSJ 약 4만 문장만으로 학습한 4층 모델이 **F1 91.3**, 준지도 설정에서 **92.7**을 기록했다. 과제별 튜닝을 거의 하지 않았는데도 RNN seq2seq가 약세를 보이던 소규모 데이터 환경에서 BerkeleyParser를 넘어섰다는 점이 의미 있다.

## 5. 비판적으로 읽은 부분

**수치의 불일치.** 초록과 Table 2는 영-불 big 모델의 BLEU를 41.8로 적고 있는데, 6.1절 본문에는 41.0으로 적혀 있다. 표와 초록이 일치하므로 본문 쪽이 오기로 보이지만, 널리 인용되는 논문에도 이런 불일치가 남아 있다는 점은 논문을 읽을 때 본문 서술만 믿지 말고 표와 교차 확인해야 한다는 교훈을 준다.

**$O(n^2)$라는 대가.** 논문은 경로 길이 $O(1)$을 강조하지만, 그 대가로 시퀀스 길이에 대해 제곱 복잡도를 진다. 문장 단위 번역에서는 $n < d$라 문제가 되지 않았지만, 문서 단위나 긴 컨텍스트로 넘어가면 이 항이 곧바로 지배적인 비용이 된다. 저자들도 restricted attention을 향후 과제로 남겨 두었고, 실제로 이후 연구의 큰 줄기가 여기서 갈라져 나왔다.

**추론은 여전히 순차적이다.** 병렬화의 이점은 학습에 한정된다. 디코더는 추론 시 토큰을 하나씩 생성해야 하므로, 순차성이라는 문제를 완전히 없앤 것은 아니다. 결론에서 저자들이 "생성을 덜 순차적으로 만드는 것"을 연구 목표로 명시한 것도 이 때문으로 보인다.

**설계 선택의 근거가 불균등하다.** 스케일링이나 멀티 헤드처럼 이유를 정밀하게 밝힌 부분이 있는 반면, $d_{ff}=2048$이나 warmup 4,000 같은 값은 근거 설명 없이 제시된다. 재현이나 변형을 시도할 때는 어떤 값이 원리에서 나온 것이고 어떤 값이 경험적으로 정해진 것인지 구분할 필요가 있어 보인다.

## 6. 교재 범위와 연결해 정리한 생각

6장의 LSTM과 7장의 트랜스포머는 결국 같은 질문에 대한 서로 다른 답이라는 인상을 받았다. 질문은 "멀리 떨어진 정보를 어떻게 현재까지 가져올 것인가"이고, LSTM은 셀 상태라는 통로를 뚫고 게이트로 그 통로를 여닫는 방식으로 답했다. 정보가 여전히 시점을 하나씩 지나야 한다는 점에서 통로는 길다. 반면 트랜스포머는 통로를 짧게 만드는 대신 모든 위치를 직접 잇는 방식을 택했고, 그 결과가 Table 1의 경로 길이 $O(1)$이다.

교재가 인코더 출력을 디코더의 키·값으로 넘긴다고 서술한 대목은, 논문의 "encoder-decoder attention" 절과 정확히 대응한다. 6장에서 본 Seq2Seq의 고정 크기 문맥 벡터 병목이 왜 문제였는지를 떠올리면, 디코더의 각 위치가 소스 전체를 매번 다시 들여다볼 수 있게 만든 이 설계의 의미가 분명해진다.

한편 교재의 수식 7.2는 $\sqrt{d}$로 나눈다고만 적고 있는데, 논문을 읽고 나서야 그 $d$가 정확히는 **키 벡터의 차원 $d_k$** 이고, 나누는 이유가 내적의 분산이 $d_k$에 비례한다는 통계적 사실에서 온다는 것을 알게 되었다. 교재만으로는 "차원이 커지면 값이 커지니까"라는 직관에서 멈췄을 부분이라, 원 논문을 함께 보는 일의 가치를 실감했다.

## 7. 토의하고 싶은 점

1. Table 3 (A)행에서 헤드 수가 너무 많을 때 성능이 떨어지는 이유는 무엇일까. 헤드당 차원($d_k = d_{model}/h$)이 지나치게 작아져 각 헤드가 표현할 수 있는 관계가 빈약해지기 때문이라고 추측하는데, (B)행에서 $d_k$ 축소가 성능을 해친다는 관찰과 같은 원인으로 볼 수 있을지 궁금하다.
2. (E)행에서 사인 위치 인코딩과 학습된 위치 임베딩의 성능이 사실상 같았는데, 그럼에도 외삽 가능성을 근거로 사인 방식을 택했다. 이 기대가 실제로 검증된 적이 있는지, 아니면 이후 상대 위치 인코딩이나 RoPE 같은 방식으로 대체되었는지 함께 이야기해 보고 싶다.
3. 교재 실습이 그리디 디코딩을 쓴 반면 논문은 빔 크기 4, 길이 페널티 $\alpha=0.6$의 빔 서치를 사용했다. 디코딩 전략의 차이가 실제 번역 품질에 얼마나 영향을 주는지 궁금하다.

---

## 참고 문헌

1. 윤대희, 김동화, 송종민, 진현두, 『(파이토치 트랜스포머를 활용한) 자연어 처리와 컴퓨터 비전 심층학습』, 위키북스. — 6장 pp. 304~335, 7장 pp. 357~387
2. A. Vaswani, N. Shazeer, N. Parmar, J. Uszkoreit, L. Jones, A. N. Gomez, Ł. Kaiser, I. Polosukhin, "Attention Is All You Need," *NeurIPS 2017*. arXiv:1706.03762
3. S. Hochreiter, J. Schmidhuber, "Long Short-Term Memory," *Neural Computation*, 9(8), 1997.
